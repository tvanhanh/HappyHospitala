import 'package:flutter/material.dart';

class SpecialtiesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Chuyên khoa')),
      body: Center(child: Text('Danh sách chuyên khoa')),
    );
  }
}

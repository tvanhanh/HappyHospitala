// ignore_for_file: file_names
import 'package:flutter/material.dart';

/// Placeholder screen for the Admin Scheduling feature.
/// This will be replaced with the full scheduling implementation in a future sprint.
class AdminSchedulingScreen extends StatelessWidget {
  const AdminSchedulingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'Scheduling — Coming Soon',
          style: TextStyle(fontSize: 18, color: Colors.grey),
        ),
      ),
    );
  }
}

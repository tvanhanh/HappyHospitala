import 'package:flutter/material.dart';

class PremiumPackageScreen extends StatelessWidget {
  const PremiumPackageScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Quản lý Gói Premium (Đang phát triển)'),
    );
  }
}

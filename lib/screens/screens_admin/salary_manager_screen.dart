import 'package:flutter/material.dart';

class SalaryManagerScreen extends StatelessWidget {
  const SalaryManagerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Quản lý Lương thưởng (Đang phát triển)'),
    );
  }
}

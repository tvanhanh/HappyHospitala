import 'package:flutter/material.dart';

class PredictionScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Dự đoán bệnh tiểu đường')),
      body:
          Center(child: Text('Chức năng dự đoán bệnh sẽ được triển khai sau.')),
    );
  }
}
